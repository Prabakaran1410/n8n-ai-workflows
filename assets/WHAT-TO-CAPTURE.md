# Screenshots this repo needs

Three images. Each has a job; none of them is decoration.

| File | Shot | Why |
|---|---|---|
| `01-invoice-intake.png` | The full canvas of workflow 01, zoomed so every node label is readable, with the error branch visible | The README hero, and the Upwork catalog thumbnail. A buyer scrolling a category recognises the node-graph shape before reading a word |
| `02-review-message.png` | The Slack message from a `review-me.pdf` run, showing the flagged ambiguous date | The one image that proves "it does not guess silently". This is the single most persuasive asset in the repo |
| `03-error-alert.png` | The Slack alert from a `boom.pdf` run, naming the node that failed | Proves the error handling is real and not a bullet point |

Export at **1600×1200 or larger**. Upwork rejects anything under 1000×750, and
1000×750 is exactly 4:3 — a 16:9 screenshot gets cropped even when it passes.

Once they exist, restore the image line at the top of `README.md`.
